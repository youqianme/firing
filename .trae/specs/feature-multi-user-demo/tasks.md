# 任务列表

- [x] 任务 1: 重构数据访问层以支持多用户
  - [x] 子任务 1.1: 更新 `DatabaseManager` 以支持用户上下文（可选，或仅将 `userId` 传递给仓库）。
  - [x] 子任务 1.2: 更新所有仓库方法（例如 `UserSettingsRepository`，`AssetRepository`）以接受 `userId` 作为参数。
  - [x] 子任务 1.3: 更新 SQL 查询以按 `userId` 过滤（WHERE id = ? 或 userId = ?）。
  - [x] 子任务 1.4: 验证现有测试是否仍然通过（或更新它们）。

- [x] 任务 2: 在前端实现用户上下文和演示逻辑
  - [x] 子任务 2.1: 创建 `UserContext` 提供者以管理当前用户状态（例如 `{ id: string, isDemo: boolean }`）。
  - [x] 子任务 2.2: 实现 `useUser` 钩子以访问用户状态。
  - [x] 子任务 2.3: 创建“演示模式”切换或初始化逻辑（例如，如果没有会话，则自动以访客身份登录）。

- [x] 任务 3: 创建演示模式的模拟数据服务
  - [x] 子任务 3.1: 创建 `mockData.ts`，包含真实的样本数据（资产、负债、设置）。
  - [x] 子任务 3.2: 实现当 `isDemo` 为 true 时加载模拟数据的机制（通过 API 拦截或特殊 API 路由）。
  - [x] 子任务 3.3: 确保演示模式是只读的或使用本地存储进行临时更改。

- [x] 任务 4: 更新 API 路由以遵循用户上下文
  - [x] 子任务 4.1: 更新 Next.js API 路由以从 Headers/Cookies 中提取 `userId`。
  - [x] 子任务 4.2: 将提取的 `userId` 传递给仓库方法。
  - [x] 子任务 4.3: 优雅地处理未经授权的访问（例如，重定向到登录或显示演示）。

- [x] 任务 5: 添加演示模式的 UI 指示器
  - [x] 子任务 5.1: 创建一个持久的横幅/警告组件，指示“演示模式”。
  - [x] 子任务 5.2: 在横幅中添加“登录 / 注册”的行动号召。
